filter:
    excluded_paths:
        - 'tests/'
        - 'vendor/'

tools:
    external_code_coverage: true

build:
    nodes:
        analysis:
            project_setup:
                override: true
            tests:
                override:
                    - php-scrutinizer-run --enable-security-analysis
